# 单日「换一换」迭代方案

## 1. 要解决的问题

当前每个日程卡片提供「这天轻松点」和「省一点」。两者虽然目标不同，但最终经常表现为减少一个停留点，用户难以理解差异，也没有得到一版真正可比较的新方案。

本轮删除两个快捷操作，合并为「换一换」：在用户已经选定的角色视角和旅行方向内，只重新生成指定当天；其他天保持不变。如果该日承载所选方向的锚点，锚点地点和核心行动必须保留。

## 2. 用户体验

每张日程卡片底部保留「查看路线」，新增一个「换一换」按钮。

点击后：

1. 按钮进入加载状态，卡片显示「正在换一版当天安排…」。
2. 系统基于当前选中的方向、用户偏好、排除项、预算和其他天内容，生成一版新的当天安排。
3. 成功后只更新该日卡片，并提示：
   - 普通日：`这一天已换一版，其他天没有改变。`
   - 锚点日：`这一天已换一版，已保留「铜锣湾 · 鹅颈街市」和对应现场行动。`
4. 失败或没有合适替代时保留原日程，说明：`暂时没找到符合当前方向的新组合，原安排没有改变。`

锁定日的「换一换」不可点击，沿用现有“先解锁再调整”的规则。

## 3. 核心规则

### P0：必须满足

- 只替换用户点击的当天，其他天逐字保持不变。
- 新方案继续遵守已选方向的角色视角、引导问题、节奏、限制条件和用户的排除项。
- 如果目标日包含 `selectedProposal.mapQuery` 或 `corePromise.commitmentId`：
  - 锚点的 `mapQuery` 不变；
  - 锚点对应的核心行动不变；
  - 锚点时间可以随新路线重新编排；
  - 其他停留点可以替换、增减或调序。
- 新旧方案至少有一个非锚点停留点发生变化；只改标题、说明、时间或费用不算“换一换”。
- 新方案仍需通过现有的格式、时间顺序、排除项、片区覆盖、锚点承诺和跨天活动范围校验。
- 生成或校验失败时不覆盖原方案。

### P1：减少重复

- 会话内记录每一天已经展示过的非锚点 `mapQuery`。
- 连续点击「换一换」时优先使用尚未展示过的地点，避免两版来回切换。
- 候选耗尽时明确告诉用户，不用旧地点伪装成新方案。

## 4. 工程改动

### 前端 `src/planner-app.js`

- 删除「这天轻松点」「省一点」按钮及对应 `dayMode`。
- 新增「换一换」，请求参数建议为：

```json
{"action":"reroll_day","day":1}
```

- 加载与结果反馈只显示在目标日卡片内。
- 成功后沿用现有日程签名比较，只清除目标日的路线缓存。
- 删除超预算提示中的「选择省一点」，改为「可以微调预算，或直接告诉我想调整什么」。

### 服务端 `planner-api.js`

- 新增独立动作 `reroll_day`，避免继续复用含义混杂的 `dayMode`。
- 校验目标日存在且未锁定，然后复制当前完整方案，仅替换 `days[day]`。
- 向模型提供：
  - `selectedProposal` 完整方向约束；
  - 当前目标日；
  - 其他天的地点与活动范围；
  - 用户偏好、排除项和预算；
  - 已召回的 `dayAlternatives`；
  - 当天已经展示过的地点。
- 如果目标日包含锚点，在提示中传入不可替换的锚点对象；返回后由服务端再次核对 `mapQuery` 和核心行动。
- 增加“有效换版”校验：去除锚点后，新旧 `mapQuery` 集合必须不同。
- 删除 `lighter/cheaper` 专用分支、路线缩短比较、费用降低比较和 `applyCheaperFallback`。

### 文档与测试

- 将手工用例 TC-14 改为「单日换一换」。
- 删除轻松点、省一点的专用自动测试。
- 增加锚点日、普通日、锁定日、重复结果和生成失败测试。

## 5. 验收标准

- [ ] 页面不再出现「这天轻松点」和「省一点」。
- [ ] 每个未锁定日显示「换一换」。
- [ ] 点击第 2 天后，第 1 天及其他天的数据完全不变。
- [ ] 普通日换版后至少一个真实地点发生变化。
- [ ] 锚点日换版后，锚点 `mapQuery`、`commitmentId` 和核心行动保持不变。
- [ ] 新方案不包含用户明确排除的地点，也不与其他天形成不合规的活动范围重复。
- [ ] 模型超时、格式错误、返回原方案或三次校验失败时，页面继续显示旧方案。
- [ ] 换版后只清除目标日地图，其他天已加载地图继续保留。

## 6. 本轮不做

- 不提供“更省”“更轻松”等预设调整目标。
- 不整趟重新生成；「换一换」只作用于点击的当天。
- 不允许通过「换一换」更换已经选定的角色方向。
- 不取消预算滑块和自然语言调整。用户仍可在输入框中提出“第二天少走一点”等具体要求。

## 7. 建议观察指标

- 换版成功率：目标 ≥ 90%。
- 锚点保留率：必须为 100%。
- 其他天误改率：必须为 0%。
- 新旧地点无实质变化率：目标 < 5%。
- 单次换版后继续再次点击的比例：用于判断新方案是否仍缺乏差异。
