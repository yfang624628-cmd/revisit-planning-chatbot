# PRD ·「约束是可以拖的」交互 Demo

**交付物**：单个自包含 HTML 文件，无构建、无依赖、双击可开。
**用途**：作品集 / 提案演示。会与携程、飞猪的真实截图并排展示。
**一句话主张**：约束不该是你说完之后的解释，应该是你能拖的东西。

---

## 0. 背景（读 `ota-ai-research-findings.md` 获取完整上下文）

实测发现两家 OTA 的智能助手都存在同一组问题：收窄提前不了、事实不当约束用、正文与商品层脱节。
飞猪已经做了一条**预算滑块**（¥780–¥5463，标注"调整为高档型酒店"），但它是**单向的**——只换酒店档次，方案本身不重组，也不说明代价。
这个 demo 要证明：同一个滑块可以做得更有用。

---

## 1. 范围

### 做
三个功能块，纵向排列在一页内，共用一套数据。

- **Block A｜预算重组条**（主体，60% 的分量）
- **Block B｜此刻筛选卡**（次要）
- **Block C｜现状对照**（静态，收尾）

### 不做（明确排除，别自作主张加）
- ❌ **地图**。理由：它不响应拖动，在这一屏里是装饰；且地图是对手主场，我们比不过也不该比。
- ❌ 真实 API / 实时库存。全部用写死的数据。
- ❌ 登录、路由、多页面。
- ❌ localStorage / sessionStorage（环境不支持）。
- ❌ 后端。

---

## 2. Block A：预算重组条

### 2.1 用户故事
> 我说了"香港两天，一个人 1500 以内"。系统给了我一个 ¥1951 的方案。
> 我想直接把预算往下拖，看它砍掉什么——而不是读一段"若您希望严格控制在1500以内，我可以帮您改成中转航班或高铁+口岸方案"。

### 2.2 数据结构

方案由 4 个**可替换部件**组成。每个部件有若干**档位**，按价格从高到低排列。

```js
const COMPONENTS = {
  transport: {
    label: '往返交通',
    tiers: [
      { id:'fly',    name:'深圳直飞香港',        price:1769, minutes:0,   note:'最省时' },
      { id:'rail',   name:'福田→西九龙 高铁',    price:430,  minutes:0,   note:'78 分钟直达' },
      { id:'bus',    name:'高铁 + 口岸巴士',     price:180,  minutes:80,  note:'每程多 40 分钟' }
    ]
  },
  stay: {
    label: '住宿',
    tiers: [
      { id:'tst',    name:'尖沙咀 海景房',       price:680, minutes:0,  note:'出门就是维港' },
      { id:'sheung', name:'上环 逸林酒店',       price:493, minutes:10, note:'近地铁' },
      { id:'ssp',    name:'深水埗 民宿',         price:280, minutes:50, note:'每天多 25 分钟通勤' },
      { id:'none',   name:'不住，当天来回',      price:0,   minutes:0,  note:'Day 2 取消' }
    ]
  },
  anchor: {
    label: '锚点',
    tiers: [
      { id:'mplus',  name:'M+ 博物馆',           price:159, minutes:0, note:'需提前预约' },
      { id:'peak',   name:'太平山顶缆车',        price:80,  minutes:0, note:'往返 88 港元' },
      { id:'daikwun',name:'大馆',                price:0,   minutes:0, note:'免费，不用订' }
    ]
  },
  food: {
    label: '餐饮',
    tiers: [
      { id:'nice',   name:'吃两顿正经的',        price:400, minutes:0, note:'' },
      { id:'mixed',  name:'茶餐厅 + 夜市',       price:250, minutes:0, note:'' },
      { id:'cheap',  name:'街边 + 便利店',       price:120, minutes:0, note:'' }
    ]
  }
};
```

**字段说明**
- `price`：人民币，单人
- `minutes`：相对最优档额外增加的时间成本（分钟，两天累计）
- `note`：一句话，不超过 12 字

### 2.3 降级序列（关键设计：可解释 > 最优解）

**不要写优化器。** 用一条写死的降级顺序，预算不够就依次降一档。这样每一步都能解释，而且行为可预测。

```js
const DOWNGRADE_ORDER = [
  { key:'food',      to:'mixed'   },
  { key:'stay',      to:'sheung'  },
  { key:'transport', to:'rail'    },
  { key:'anchor',    to:'peak'    },
  { key:'stay',      to:'ssp'     },
  { key:'food',      to:'cheap'   },
  { key:'transport', to:'bus'     },
  { key:'anchor',    to:'daikwun' },
  { key:'stay',      to:'none'    }
];
```

**算法**：从全最高档开始（总价 ¥3249）。若总价 > 当前预算，按序列依次执行降级，每降一档重算，直到总价 ≤ 预算或序列耗尽。

**兜底**：序列走完仍超预算（预算 < ¥300），显示 `装不下` 状态——见 2.6。

### 2.4 交互

- 一条滑块，范围 **¥300 – ¥3300**，步长 ¥50，初始值 **¥1500**
- 拖动时**实时**重算（`input` 事件，不是 `change`）
- 四张部件卡实时更新；发生变化的那张要有一次短促的高亮（180ms 内完成，不要弹跳）

### 2.5 输出（这是全 demo 最重要的部分）

拖动后，除了方案本身，必须显示一行**取舍说明**，句式固定：

> 为了省 ¥{省下的钱}，{做了什么}，代价是{时间代价}。

示例：
- 「为了省 ¥213，住宿从上环换到深水埗，代价是每天多 25 分钟通勤。」
- 「为了省 ¥250，交通从高铁换成高铁+口岸巴士，代价是每程多 40 分钟。」
- 「为了省 ¥280，取消住宿改当天来回，代价是 Day 2 整天没了。」

**多步降级时**，逐条列出，最多显示 3 条，超出折叠为「…还调整了 N 项」。

**文案铁律**：
- 每一条都必须说出**代价**。只说省了多少钱是不合格的。
- 不用感叹号，不用"为您"，不用 emoji。
- 若某次降级没有时间代价（如餐饮降档），代价写具体的东西：「代价是不吃那顿正经的」。

### 2.6 三个特殊状态

| 状态 | 触发 | 表现 |
|---|---|---|
| **有富余** | 预算 > 当前方案总价 + 200 | 显示「还剩 ¥{余额}，可以升一档：{下一个可升级项}」，带一个「升级」按钮 |
| **刚好** | 预算 ≈ 总价（±200） | 正常显示 |
| **装不下** | 序列耗尽仍超预算 | 方案区变灰，显示「¥{预算} 装不下香港两天。最低要 ¥{最低总价}。要不要看当天来回？」——**明说做不到，不要伪造一个假方案**（这一条直接呼应飞猪的诚实做法，是加分项） |

### 2.7 行程条（替代地图）

方案卡下方，一条横向行程条，跟着滑块变：

```
Day 1  [扶梯 30] [大馆 90] [中环街市 30] [通勤 +25] [富余 55]
Day 2  [旺角 60] [星光大道 40] [庙街 90] [富余 50]
```

- 每天总长固定 **600 分钟**（10 小时可用）
- 站点用实色块，**通勤增量用灰色斜纹**，富余用浅色留白
- 降级导致 `minutes` 增加时，灰色块变长，富余被吃掉——**"省下的钱是用时间换的"这件事要在条上肉眼可见**
- 当 `stay = none` 时，Day 2 整条变灰并打上「取消」

这个组件是地图的替代品，也是 Block A 的视觉高潮，值得多花点时间。

---

## 3. Block B：此刻筛选卡

### 3.1 用户故事
> 我在中环，还有三小时。系统知道泰昌 19:30 关门、九记周日休息——但它只是把这些写出来给我读。

### 3.2 数据

```js
const SPOTS = [
  { name:'大馆',        type:'景点', walk:9,  open:'10:00', close:'20:00', price:0,   free:true,  note:'红砖建筑群，室内外都有' },
  { name:'中环街市',    type:'景点', walk:6,  open:'10:00', close:'20:00', price:0,   free:true,  note:'百年菜市场改的文创空间' },
  { name:'半山扶梯',    type:'路线', walk:3,  open:'06:00', close:'24:00', price:0,   free:true,  note:'10:20 后为上行方向' },
  { name:'中环摩天轮',  type:'门票', walk:5,  open:'11:00', close:'22:00', price:18,  free:false, note:'转 3 圈约 15 分钟' },
  { name:'天星小轮',    type:'船票', walk:5,  open:'06:30', close:'23:30', price:5,   free:false, note:'10 分钟横渡维港' },
  { name:'泰昌饼家',    type:'小食', walk:5,  open:'09:30', close:'19:30', price:23,  free:false, note:'牛油皮蛋挞' },
  { name:'沾仔记',      type:'餐厅', walk:4,  open:'11:00', close:'21:30', price:64,  free:false, note:'只卖三样浇头' },
  { name:'九记牛腩',    type:'餐厅', walk:7,  open:'12:30', close:'22:30', price:82,  free:false, note:'周日休息', closedOn:[0] }
];
```

> 全部取自携程问道与飞猪的真实回答，**一个新地方都没加**。这一点要在页面上写出来。

### 3.3 交互

- 一条**当前时刻**滑块：08:00 – 23:00，步长 30 分钟，初始 15:20
- 一个**星期**切换：周六 / 周日（两个按钮）
- 拖动时，每张卡实时判定：
  - **可用**：正常显示，右下角显示「还剩 Xh Ym」（距关门）
  - **快关了**：距关门 < 60 分钟，加橙色标记「Xx 分钟后关」
  - **不可用**：已关门 / 未开门 / 当天休息 → 卡片降到 30% 不透明度 + 一条删除线，右下角写明原因（「19:30 已关」「今天休息」）
- 顶部实时统计：「此刻 8 个候选里，**{N} 个去不了**」——这个数字是这一块的论点

### 3.4 卡片形态

每张卡三行数据，与 Block A 的部件卡视觉统一：

```
大馆                                    [景点]
红砖建筑群，室内外都有
─────────────────────────────────────
步行 9 分钟  │  20:00 关  │  免费 · 不用订
```

- 有价格的显示价格 + 一个「订票」按钮（灰色不可点，标注"演示"）
- 免费的显示「免费 · 不用订」，无按钮
- **这一处顺带演示了正文↔商品层的 join**

---

## 4. Block C：现状对照

静态，无交互。三行数字，收尾用。

| | 携程问道 | 飞猪 | 本 demo |
|---|---|---|---|
| 预算不满足时 | 未测到预算约束能力 | 一段文字说明 + 三条建议 | 直接拖，方案重组 |
| 预算滑块 | 无 | 有，但只换酒店档次 | 有，四个部件联动 + 说明代价 |
| 营业时间 | 写在正文里 | 写在正文里 | 参与筛选 |
| 收窄提问出现在 | 第 1,380 字 | 回答末尾 | 第一屏 |

下方一段小字：

> 三块内容的推荐结果全部来自携程问道与飞猪的真实回答，未新增任何地点。
> 价格与营业时间为实测截图数据，未接实时库存。

---

## 5. 视觉与文案

### 视觉
- **不要**做成携程蓝或飞猪紫的仿品——这是我们的作品，不是仿制品
- 需要一套克制、偏工具感的配色。避开三种 AI 默认长相：奶油色+衬线体+陶土橙、纯黑+荧光绿、报纸式细线密排版
- 字体用系统 CJK 栈；数字用等宽字体（滑块数值、价格、时长全部等宽，拖动时不能跳动）
- 桌面端两栏或单栏居中皆可，最大宽度 ~1000px；<900px 单栏堆叠
- 支持 `prefers-reduced-motion`

### 文案
- 不用感叹号、不用 emoji、不用"为您"
- 取舍说明必须包含代价
- 做不到的时候明说做不到

---

## 6. 验收标准

功能：
1. 拖预算滑块，四张部件卡实时变化，**不卡顿**
2. 每次变化都出现取舍说明，且**包含时间或体验代价**，不只是省了多少钱
3. 行程条上，通勤增量的灰色块随降级变长，富余相应缩短
4. 预算 < ¥300 时进入「装不下」状态，明说最低价，**不伪造方案**
5. 预算有富余时给出可升级项和「升级」按钮
6. 拖时刻滑块到 20:00，泰昌饼家变灰划掉并显示「19:30 已关」
7. 切到周日，九记牛腩变灰并显示「今天休息」
8. 顶部「N 个去不了」的数字随滑块实时变化

工程：
9. 单文件，双击可开，无 console 报错
10. 无 localStorage / sessionStorage
11. 375px 宽度下不横向溢出

---

## 7. 优先级

| P | 内容 |
|---|---|
| **P0** | Block A 全部（滑块、降级、取舍说明、行程条、三种状态） |
| **P0** | Block B 的时刻滑块与失效判定 |
| P1 | Block B 的周日切换、商品按钮 |
| P1 | Block C 对照表 |
| P2 | 升级按钮、高亮动画 |

**如果时间只够做一件事，做 Block A 的「取舍说明」。** 那句话是整个 demo 的论点，其余都是它的舞台。
