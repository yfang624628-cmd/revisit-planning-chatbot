# 行程规划助手：小步迭代建议报告

日期：2026-09-13  
性质：产品建议与实施记录。依据当前 README、前后端实现及本次对话中的使用反馈；未开展独立用户研究，收益判断属于待验证假设。

## 实施状态（2026-09-13）

已完成第一轮：阶段提示随状态更新；单日调整在卡片内显示实际变化并保留其他天地图；预算统一说明为当地行程预算；地图支持完整、部分、单点、零点和道路不连通状态，并识别路线中的渡轮路段；组合地名首选项失败后会尝试另一半。住宿与餐饮生成提示改为“一个建议加一个理由”。固定“重点”标签已移除。

“每日轮廓折叠”暂未实施，先观察当前信息密度；预算是否包含往返大交通目前通过固定口径说明解决，尚未新增独立槽位。

## 1. 这一轮要解决什么

核心目标保持不变：帮助用户按照预算，生成包含每日地点、路线、住宿与餐饮建议的可交互行程。

目标用户已有出行意向，但不愿意反复读攻略、比较、重新整理。他需要先看到一版，再通过几次明确的调整得到合适的安排。

当前产品已经具备这个基础：自然语言输入、条件确认、每日卡片、预算微调、单日调整、整天保留和地图。这一轮建议把这些能力连贯起来：**先看清安排，再理解调整，最后知道哪些信息可以参考。**

这一轮不新增业务模块、不更换模型或地图供应商、不重做视觉，也不恢复封板 Demo。此前提过的完整偏好问卷、单地点锁定、商家实时报价和天气接入，都暂缓。

## 2. 从当前实现看到的具体问题

| 已观察到的现状 | 用户可能遇到的问题 | 本轮处理方向 |
| --- | --- | --- |
| 生成成功后，前端仍可能保留确认前的回复 | 已经看到行程，却还被提示“核对一下就能生成” | 反馈跟随当前阶段 |
| 每天第一个地点固定显示“重点” | 出场顺序被误认为推荐优先级或用户选定锚点 | 删除未经判断的标签 |
| 默认不含往返大交通，但总估算直接与输入预算比较 | 用户可能把“当地花费够用”理解成“整趟旅费够用” | 在确认与结果两处统一预算范围 |
| 单日调整仍会整体重绘页面，任意日程变化会清空所有地图 | 用户失去阅读位置，没改的地图也消失 | 调整落在当天，保留无关状态 |
| 地图已有标准检索名，但并未强制校验；旧名称靠简单裁剪，搜索取首个有效坐标 | 组合地点、同名地点可能失败或误配 | 补足定位约定，区分展示与检索 |
| 地图不足两点即整体报错，部分成功时仍突出路线总距离 | 有用的单点被隐藏，部分距离可能被当作全天距离 | 展示成功部分，明确覆盖范围 |

这些是实现和现有反馈支持的问题；“用户会更满意”仍需实际验证。尤其要修正一个表述：当前搜索只是把城市拼入查询，不能称为已经严格限制在对应城市。

## 3. 六项可选改进

### 建议一：让页面只说当前这一步需要说的话

**用户体验：**首次进入保留“告诉我你的出行需求”和现有示例；条件不足时只追问缺项；条件齐全后突出确认卡；生成完成后撤下确认阶段提示，让行程成为主内容。

结果阶段把输入框收紧为“想改哪里？”，继续支持普通提问。生成成功可以短暂显示“已生成 3 天安排”，无需长期占据一块大面积提示区域。

**巧思：**同一个输入框随着任务进展换角色，不增加向导页面，也不让用户在表单与聊天之间选模式。

**最小实现：**根据是否缺槽位、是否已确认、是否已有行程决定提示；确认成功使用当次反馈；保留已有输入框与确认表单。

**验收：**生成后不再出现“等待确认”；缺预算时仍不能生成；追问“适合带爸妈吗”仍保留原行程。

**改动量：小。**主要涉及 `src/planner-app.js` 的反馈与布局。

### 建议二：先给每天的轮廓，细节留在卡片里

**用户体验：**每张卡先露出“第几天、城市、主题、地点顺序”，例如：

> 第 2 天 · 东京｜老街与公园  
> 谷中银座 → 上野公园 → 阿美横丁  
> 展开当天安排

展开后再显示现有时间、理由和操作；住宿、餐饮、交通继续沿用现有展开区。短时行程只有一张卡，可默认展开，不必照搬多日布局。

去掉第一个地点固定显示的“重点”。当前并没有独立的锚点选择与校验，不应把第一站包装成用户已确认的锚点；本轮先让地点顺序清楚可见。

**巧思：**内容仍一次生成，用户分层阅读就能获得逐步展开的感觉，无需增加模型调用和确认步骤。

**验收：**五日行程可以先扫读五天主题与地点；展开不调用模型；修改当天后当天保持展开。

**改动量：小。**复用日程数组与现有卡片，增加本地展开状态。

### 建议三：每次调整，只告诉用户“哪里变了”

**用户体验：**点击第二天“这天轻松点”，加载状态就在第二天；完成后该卡短暂突出变化，显示：

> 第 2 天已调整：3 站变为 2 站，费用估算减少 ¥120。

数值必须由新旧数据计算；若费用没变就不显示费用变化。如果只是住宿建议变化，显示“住宿建议已更新”，不编造省钱原因。新旧内容相同时显示“本次安排没有变化”。

**巧思：**让用户只检查改动部分，不必重新读完整行程。这也直接体现“交互式生成”的价值。

**最小实现：**请求前保留用于比较的旧数据；按天比较地点、住宿、餐饮和费用；保留未修改日的地图、展开状态和输入草稿。模型调用期间保持现有互斥规则，避免同时修改。

地图返回还要绑定请求时的日程版本；若用户已重新开始或行程已变，不把旧地图挂到新卡片上。

**验收：**修改第二天，第一天的保留状态与地图不变；加载和结果反馈都在第二天；失败保留原方案；普通问答不出现“行程已更新”。

**改动量：小到中。**前端状态与差异比较为主，不增加撤销系统或单地点编辑。

### 建议四：让预算滑块表达一个清楚的决定

**用户体验：**确认卡明确本版估算范围：“住宿、餐饮、当地交通和游玩；不含往返出发地的机票/火车。”如果用户明确给的是含机票的总预算，先追问其中准备留给当地行程多少，不能静默拿总预算来比较。

结果卡使用“当地行程估算”和“当地预算”，在展开区汇总已有四类费用。滑块拖动时显示：

> 当地预算：¥8,000 → ¥8,300（全员）  
> 按新预算调整

提交前，行程估算保持不变。提交成功后按实际结果更新；增加预算不等于必须把钱花完，也不能宣称价格已经核实。

保留原输入上下 ¥500 的规则，不增加消费档位。每人预算情况下明确写“每人”，并同时展示全员额度，避免人数放大后不易察觉。

**巧思：**只利用现有金额和费用分类，让滑块从一个数字控件变成用户看得懂的调整意图。

**验收：**含往返交通的总预算不能直接判定“够用”；滑动不调用模型、不改变估算；合计来自每日费用；全员与每人单位始终可见。

**改动量：小到中。**确认流程、预算文案、费用汇总及模型提取约定需要一起调整；无法可靠识别预算范围时保留追问。

### 建议五：地图允许部分有用，但不能假装完整

**用户体验：**用结果说明代替技术报错，按四种状态呈现：

| 查询结果 | 页面表现 |
| --- | --- |
| 所有地点定位且有道路路线 | 显示路线与“步行连接约 X 公里” |
| 只有部分地点定位成功 | 显示“已定位 2/3 站”；距离标为“已定位地点间步行距离”，明确不含缺失站 |
| 只有一个地点定位成功 | 保留地图单点，不显示路线距离；说明其他地点暂未匹配 |
| 坐标齐全但道路连接失败 | 保留地点标记，说明“暂未找到步行连接”；不显示推测的道路和时长 |

零个地点成功时，显示“这几个地点暂时没匹配到，行程仍可查看”。网络错误提供重试；名称无法匹配时，不让同一个无效查询成为唯一出路。

**最小实现：**先落实已有 `mapQuery` 的格式校验与标准名称要求。旧数据缺字段时，可按需让现有模型补标准地名一次，只用于检索、不改行程；之后仍由地图数据定位。重试有上限，不靠无限调用碰运气。

对明显不符城市或属于整条河流等宽泛对象的匹配，标为待确认；这些检查只能降低误配，不能证明地点正确。无法判断的匹配不得称为“已核实”。不要通过删除地名词语、把河流替换为公园来强行得到结果。

保留行程原编号：第 2 站缺失时，地图展示 1、3，并说明缺少第 2 站。完整匹配地址默认收起，避免占据主内容。

**巧思：**把失败分解成“哪些部分还可用”，既保留地图价值，也避免一条漂亮折线制造错误信心。

**验收：**用纽约组合地名、单站、缺失中间站、道路不连通和网络超时分别测试；失败不换景点；不把部分距离写成全天距离；地图加载不触发行程重写。

**改动量：中，六项中最大。**需要调整路线响应和渲染分支；本轮不增加导航、交通方式选择或新地图服务。自由女神像等特殊地点不得仅凭坐标就承诺可连续步行。

### 建议六：让“住哪里、吃什么”更容易做判断

**用户体验：**保留当前文本建议和展开方式，只收紧内容组织：先写一个主要建议，再写一个与本次需求有关的理由。

示例仅说明格式，不代表事实已验证：

> 住宿：优先住上野附近。与这版行程的活动区域更集中。  
> 餐饮：当天以沿途简餐为主。减少专门绕路找餐厅。

能有把握给具体候选时再给，不能为了填卡片硬编商家。不扩展成酒店或餐厅列表。用户想改时继续用现有输入框说“第二天吃得好一点”。

短时行程沿用当前不需住宿的规则，直接隐藏住宿项；交通建议与“查看路线”靠近摆放，方便用户同时理解文字与地图。

**巧思：**通过信息顺序减少筛选负担，同时保留自然语言的自由度。

**验收：**建议理由与本次行程有关；正文不重复费用金额；短时卡不展示“住宿：无需住宿”；无把握的具体商家不强行输出。

**改动量：小。**主要调整生成提示与现有展开区，不新增字段体系或商家接口。

## 4. 推荐执行顺序与取舍

| 批次 | 做什么 | 为什么先做 | 完成边界 |
| --- | --- | --- | --- |
| 第一批 | 建议一、三、四 | 看清当前阶段、知道改了什么、理解钱算到哪里，是所有用户都会经过的过程 | 现有输入—确认—生成—调整流程连续走通 |
| 第二批 | 建议五 | 地图已有明确失败反馈，需要修复可信度 | 可解释地展示部分结果，定位失败不影响行程 |
| 第三批，按试用反馈决定 | 建议二、六 | 阅读密度与建议形式需要观察用户偏好 | 先改默认展开方式和文案，不追加业务模块 |

改动量为相对判断，不是排期承诺。地图的地名质量受模型与数据覆盖影响，应单独验收，不能用“接口返回成功”代替地点匹配正确。

本轮尤其不建议把生成拆成“先选锚点、再选酒店、再选餐厅”的强制多步流程：这会增加等待和确认成本，对已有明确需求的用户也不合适。先尝试建议二的阅读分层，观察是否足够。

不恢复长篇“取舍说明”。建议三只报告数据中实际发生的变化，不解释模型猜测的因果，也不展示“省了 ¥0”之类无用内容。

## 5. 用很小的试用验证价值

复用当前六个首页示例，不新增数据平台。先做固定场景验收，再找约 5 位试用者完成：生成一版、调整一天、理解预算、查看一天地图。记录观察即可。

| 要验证的假设 | 观察方式 | 进入下一轮的依据 |
| --- | --- | --- |
| 阶段提示更清楚 | 是否知道下一步做什么，是否误以为还需确认 | 不再因过时提示停住 |
| 局部反馈降低检查成本 | 调整后能否直接指出改了哪天、哪些内容 | 不必从第一页重新找差异 |
| 预算口径可理解 | 请用户复述估算是否含往返交通、是全员还是每人 | 固定验收场景均能正确回答；误解则继续改口径 |
| 部分地图有用且不误导 | 请用户指出缺失站、解释距离包含哪些地点 | 不把部分路线当成全天完整路线 |
| 折叠减少阅读负担 | 观察展开、来回寻找内容的行为 | 与当前版本比较；若频繁迷路就保留更多默认信息 |

整体看“完成一次满意调整所需时间”和“过程中需要别人解释的次数”。不把停留更久、点击更多当成成功。小样本只用于发现障碍，不声称统计显著提升。

## 6. 本轮完成后的体验

用户说完需求，核对人数与预算范围，看到每天的安排；点击某天调整，变化就在那张卡里；看地图时知道哪些地点已匹配、距离包含什么。整个过程沿用现有单入口、模型生成和卡片交互。

这一轮的产品巧思，是让每一步都减少一点用户的判断负担：页面及时退出过时状态，调整只突出变化，预算把口径说清，地图保留有用部分。是否值得继续增加能力，等这条现有流程用顺之后再判断。

## 附：评估依据

- `README.md`：产品目标、现有能力与明确边界。
- `src/planner-app.js`：条件确认、阶段回复、预算展示、固定重点标签、单日操作与地图状态。
- `planner-api.js`：预算与锁定校验、地名处理、地图查询及短时行程约束。
- `src/examples.js`：现有六个可复用试用场景。
- 本次对话：用户对纽约地点无法定位、重复提示、长篇说明和较大改造的反馈。

以上建议以产品脑暴 skill 的问题拆解、假设检验与小成本验证方法组织；文件描述的是建议方案，不代表已实施或已完成真实接口验证。
