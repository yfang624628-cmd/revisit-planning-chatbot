# 角色旅行规划：召回、排序与生成改进指导

日期：2026-09-15

本文区分当前代码事实与建议方案。2026-09-15 已落地第一轮 P0/P1 工程改动；尚未进行真实模型效果评测。

### 第一轮实施状态

- 召回结果增加兴趣、片区与节奏分项；明确“不想走、少走、行动不便”等要求时过滤步行较多的素材。
- 方向模型允许返回 0–3 张和不足原因；取消模板补卡，模型故障不再伪装成候选不足。
- 服务端要求角色关键词与素材标签存在交集，拒绝缺字段、无有效行动或低适配组合。
- 每张卡生成结构化 `corePromise` 和证据记录；服务端在找到日程锚点后绑定承诺 ID，并确保说明完整呈现核心行动，模型不负责内部 ID。
- 前端支持方向为零的诚实空状态；README 已同步三城各 32 条素材及新生成机制。

## 1. 产品目标与汇报口径

面向有旅行经验、有自身偏好、尚未把兴趣组织成行程的人，通过角色的观察与取舍方法形成旅行方向，再用对话和交互落实路线。城市再访者作为当前验证切口；是否放开首次到访，应另行决定，当前代码仍要求确认去过该城市。

角色负责内容为何值得选择；交互负责帮助用户比较、选择和调整。角色必须影响选点理由、现场行动与路线组织。核心体验保持连贯，餐饮和交通不必强行附会主题。

### 可直接分享的两个工程 bullet（当前实现）

- **召回与排序：**将自然语言需求提取为结构化条件，从本地城市素材库按城市筛选，排除字面命中的禁选内容及刷新时已展示的素材；兴趣、喜欢和重温内容每次命中加 3 分，片区命中加 6 分，轻松偏好根据体力描述加 2 或减 3 分，明确少走时过滤步行较多素材；记录各项排序理由，按总分降序、同分按稳定顺序排列，最多向模型提供 16 条候选。
- **约束式两阶段生成：**先将候选素材、15 个角色的关注点与观察方法、5 类行动以及用户条件交给模型，生成 0–3 张“角色 × 地点素材 × 行动类型”的方向卡；服务端要求角色与素材有标签证据、校验字段和 ID 并做批内去重，不足时不补卡。用户选定方向后，系统生成稳定的核心承诺 ID；日程锚点必须绑定该 ID，并在地点说明中完整兑现核心行动。

## 2. 当前机制的准确解释

### 召回对象

当前香港、上海、柏林各有 32 条素材。素材不是完整固定日程，也不只是单个 POI：每条包含一个锚点、周边内容、主题标签、体力描述、费用档位、新鲜感和限制。README 的各 8 条已过时。

### 召回和排序链路

1. 从会话获得城市、片区及 revisit 偏好。
2. `generationContext` 将片区作为独立排序信号，刷新时把已展示方向 ID 还原为素材 ID。
3. `materialCandidatesFor` 按城市与已展示 ID 过滤。
4. 将标题、锚点、周边内容、标签、新鲜感、限制拼成文本；禁选词被包含则排除。
5. 对兴趣、liked、revisit 和 direction 逐项执行 `includes`，每命中一项加 3 分；片区命中加 6 分；轻松偏好根据体力描述加减分，明确少走时过滤步行较多素材。长度不超过 1 的匹配项忽略。
6. 按分数降序，同分按稳定散列排序；截取最多 16 条。

这是字面匹配，没有语义向量召回或学习排序。零分素材仍可进入候选。同义表达或长句不一定命中。预算和时间尚未在这里形成结构化可行性过滤；体力只处理少量明确措辞，片区也是加分信号，不能称为严格地理过滤。

### 生成与质量门槛

模型在候选范围内选素材、角色和行动类型，并生成文字。服务端要求组合至少有一个角色关键词与素材标签交集，检查字段、行动 ID 和批内重复；模型返回顺序用于卡片展示，当前没有独立质量重排。模型可以返回 0–3 张与不足原因，代码不再补卡。模型调用失败时整次操作失败并保留原会话状态。

每张通过的方向卡都会生成 `corePromise`，记录核心行动、行动类型、锚点和稳定承诺 ID。最终日程由另一次模型调用生成；服务端确认锚点存在后自行绑定承诺 ID，并把缺失的核心行动补入锚点说明。内部 ID 不再要求模型复述。其他日程地点仍不代表已经经过事实核验。

素材的兴趣命中每项 +3、片区命中每项 +6；轻松偏好会对合适素材 +2、步行较多素材 -3，明确少走时会直接过滤步行较多素材。角色关键词与素材标签的匹配用于组合准入，目前最低分为 2。

## 3. 优先级 P0：让“不合适就少给”成立

### 修改目标

区分模型故障、事实资料不足、条件不满足和本轮素材耗尽。减少低质量补卡，避免用户把能力边界误认为城市本身没有内容。

### 建议实现

- 模型可返回 0–3 张卡，并附结构化不足原因；服务端核对原因与候选情况，不把模型判断当成客观事实。
- 已取消默认补满三张。故障时保留原状态并允许重试；后续如引入兜底卡，也必须通过同样质量门槛。
- 不足时表述为“当前资料中没有找到足够支持这个方向的内容”，或明确具体冲突。
- 返回少量可行替代方向，并写明需要改变的条件。用户明确排除项不得自动放宽。

### 验收

只剩一个合格方向就展示一个；低适配角色不因需要补位被使用；模型故障不会被解释成目的地无合适体验；放宽条件前不改变用户硬约束。

## 4. 优先级 P0：把角色变成可检查的策划依据

### 修改目标

形成“角色关注问题 → 地点事实支持 → 现场行动 → 路线联系”的完整链条。

### 建议实现

- 素材增加可引用的观察线索、事实来源、核对日期、适用条件和未知项。不能用模型生成的描述反过来证明事实。
- 角色增加适配证据要求与不适合的情况。关键词命中可以用于初筛，不能作为充分依据。
- 方向卡内部记录：回应的用户需求、核心体验承诺、支持该承诺的素材事实 ID、对应行动及不可替代部分。
- 每个核心行动明确发生地点、观察对象和行动方式；不强制所有配套内容角色化。
- 规则校验 ID、必需字段和约束；角色逻辑由人工样本评审起步，模型评审仅辅助，不能把自评分当事实核验。

### 验收

删除角色名称后仍能辨认策划方法；同一个素材换角色时，关注的问题与行动有实质差异；缺少事实支撑时不编造现场细节。

## 5. 优先级 P0：选定方向在日程和调整中得到兑现

### 当前风险

`validateRevisitPlan` 通过 mapQuery 检查锚点，通过行动文本与日程的局部词语重叠检查兑现；“轻松”部分用站点数量近似。通过这些检查不能证明动作可执行或实际步行轻松。

### 建议实现

- 将核心承诺与具体 stop/活动记录关联，记录地点、行动、所需停留时长与必要条件。
- 对可结构化的承诺直接检查，例如锚点、时间覆盖、必需交通、用户硬排除项；缺乏可靠数据时保留未知状态。
- 用户修改预算或体力要求后，先检查核心承诺能否保留。冲突时给出明确取舍，不能默默替换体验。
- 保持校验成功才提交新方案的原则，失败时保留原方案。

### 验收

有锚点但无核心行动不能通过；删掉体验关键环节不能只靠标题保留而通过；减少站点不能直接宣称减少步行距离；锁定和未要求修改的内容不被无故重写。

## 6. 优先级 P1：提升召回和排序的需求响应

### 先完善结构，再决定是否引入语义检索

当前每城 32 条素材，可以先遍历城市候选，不必为了使用向量数据库而增加系统复杂度。

1. 规范化需求：把用户原话映射为可解释的兴趣和限制，保存原文与来源；保留推测和明确要求的区别。
2. 规范化素材：增加片区、可用时长范围、步行负担、转场、费用范围与事实支持字段；未知不伪装成满足。
3. 先检查已知硬约束，再比较兴趣匹配、角色证据、交通负担与体验差异。零关键词分不必直接排除，但必须有其他可解释的适配依据。
4. 保存各项排序理由。初始规则用人工案例校准，不把未经验证的权重或总分展示为匹配准确率。
5. 最终选卡考虑相互差异，避免三个方向只换角色名称。多样性只在适配合格后考虑。

若评测表明同义表达漏召回严重，再增加语义召回，与字面匹配合并、去重、统一检查条件。若问题是库内根本没有合适素材，则需要扩展有来源的内容；改排序无法创造覆盖。

## 7. 优先级 P1：把刷新改成有原因的探索

- 明确“不想再去这个地点”与“不喜欢刚才的玩法”；前者排除地点，后者可以保留地点探索其他有依据的体验。
- 已展示但未拒绝的地点以降低优先级处理；用户要求全新地点时再严格排除。
- 记录拒绝原因，如距离、体力、主题或重复经历，用于下一轮召回与排序。
- 检查地点、行动和核心问题的重复，而不只检查素材 ID。没有合格的新方向时说明边界。

## 8. 验证与推进顺序

先补召回/生成可观察记录，再处理 P0 的少给机制、角色证据和承诺兑现，随后完善 P1 排序与反馈探索。最后依据评测决定扩素材还是加语义检索。

建议准备约 12 组需求，每组改变一个关键变量；保留当前版本作对照。覆盖同义表达、明确排除、片区限制、低体力、角色不适配、候选不足、模型失败、预算调整、愿意重温与拒绝玩法。

记录候选 ID、入选/排除理由、排序分项、生成来源、卡片核心承诺、失败原因；避免在调试记录中无必要保存个人信息。

衡量：硬约束违反数、有效方向比例、角色合理性、关键需求变化后的实际响应、方向承诺兑现情况、体验重复情况。用户侧观察是否能选定方向及选定理由，不能用“文字更丰富”替代效果。

探索性测试可邀请约 5 位目标用户，交替展示相同信息条件下的普通行程与角色方案。这用于发现问题，不作为已证实市场需求的结论。

## 9. 工程入口

- `revisit-catalog.js`：素材结构、字面召回、排序。
- `content-store.js`：片区注入、已展示过滤、候选截取。
- `content/content-v1.json`：角色方法、行动类型、数量与去重配置。
- `planner-api.js`：提取需求、方向生成与质量门槛、方向选择及行程承诺校验。
- `src/planner-app.js`：方向反馈、不足原因、调整结果表达。
- `test/planner.test.js`：按上述失败案例补充有行为意义的验证。

本方案不预设扩城、增加角色数量、引入预订系统或账号体系。先验证核心策划机制，再决定范围。
